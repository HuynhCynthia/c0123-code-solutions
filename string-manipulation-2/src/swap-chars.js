/* exported swapChars */
/* function swapChars(firstIndex, secondIndex, string) {
  var firstChar = string[firstIndex];
  var secondChar = string[secondIndex];
  var firstStringSwitch = string.replace(string[0], secondChar);
  var secondStringSwitch = firstStringSwitch.replace(string[1], firstChar);
  console.log(secondStringSwitch);
} */
